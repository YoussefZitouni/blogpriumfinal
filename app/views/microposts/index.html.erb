<p id="notice"><%= notice %></p>

<h1>Liste Microposts</h1>

<table>
  <thead>
    <tr>
      <th>Contenu</th>
      <th>Utilisateurs</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @microposts.each do |micropost| %>
      <tr>
        <td><%= micropost.content %></td>
        <td><%= micropost.user_id %></td>
        <td><%= link_to 'Voir', micropost %></td>
        <td><%= link_to 'Modifier', edit_micropost_path(micropost) %></td>
        <td><%= link_to 'Supprimer', micropost, method: :delete, data: { confirm: 'Vous êtes sur?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouveau Micropost', new_micropost_path %>
